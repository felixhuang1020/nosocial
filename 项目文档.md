我来为你生成一份完整的系统架构与开发文档，涵盖小程序端和商家管理端的所有细节。这份文档将包含具体的数据库表结构、API 接口定义、页面组件划分、核心业务逻辑流程，可直接作为 AI 编程的 Prompt 使用。

---

# NoSocial 酒吧小程序 & 商家管理系统 —— 全栈开发文档

## 一、项目概述

### 1.1 产品定位
面向酒吧场景的微信小程序，包含顾客端（小程序）和商家管理端（Web），支持共享股东分销、塔罗酒水推荐、生日营销、离店点评返券等核心功能。

### 1.2 技术栈选型

| 层级 | 技术选型 | 说明 |
|------|---------|------|
| 小程序端 | 微信小程序原生 + TDesign 组件库 | 符合用户既有技术积累 |
| 管理端前端 | Vue 3 + Vite + Element Plus + Pinia | 企业级中后台标准技术栈 |
| 后端服务 | Go 1.22+ + Gin + GORM + Zap | 高性能、强类型、易部署 |
| 数据库 | MySQL 8.0（主库）+ Redis 6.x（缓存/会话） | 事务支持 + 高性能读取 |
| 对象存储 | 阿里云 OSS / 腾讯云 COS | 图片、轮播图、塔罗牌面存储 |
| 支付 | 微信支付 JSAPI | 共享股东注册费、酒水订单 |
| 部署 | Docker + Docker Compose / K8s | 容器化部署，支持弹性伸缩 |

---

## 二、系统架构设计

### 2.1 服务拆分（单体架构，模块化设计）

```
┌─────────────────────────────────────────┐
│              微信小程序端                │
│  (首页/展示/酒水单/我的)                │
└──────────────┬──────────────────────────┘
               │ HTTPS/WSS
┌──────────────▼──────────────────────────┐
│           Nginx 网关层                  │
│  (SSL终结/限流/负载均衡/静态资源)        │
└──────────────┬──────────────────────────┘
               │
┌──────────────▼──────────────────────────┐
│           Go Gin 后端服务               │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ │
│  │ User    │ │ Share   │ │ Tarot   │ │
│  │ Module  │ │ Holder  │ │ Drink   │ │
│  └─────────┘ └─────────┘ └─────────┘ │
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ │
│  │ Birthday│ │ Review  │ │ Drink   │ │
│  │ Gift    │ │ Coupon  │ │ Menu    │ │
│  └─────────┘ └─────────┘ └─────────┘ │
│  ┌─────────┐ ┌─────────┐             │
│  │ Admin   │ │ Upload  │             │
│  │ Module  │ │ OSS     │             │
│  └─────────┘ └─────────┘             │
└──────────────┬──────────────────────────┘
               │
    ┌──────────┴──────────┐
    ▼                     ▼
┌─────────┐         ┌─────────┐
│  MySQL  │         │  Redis  │
│  8.0    │         │  6.x    │
└─────────┘         └─────────┘
```

### 2.2 模块职责说明

| 模块 | 职责 |
|------|------|
| `user` | 微信登录、用户信息、共享股东身份、生日信息 |
| `shareholder` | 邀请码生成、佣金计算、注册费支付、下级绑定 |
| `tarot` | 78张塔罗牌数据、占卜算法、酒水匹配规则 |
| `birthday` | 生日判断、礼品发放记录、核销 |
| `review` | 大众点评跳转、截图上传审核、减免券生成 |
| `drink` | 酒水分类、酒水详情、酒水单管理 |
| `banner` | 轮播图管理、展示图管理 |
| `admin` | 商家登录、数据统计、订单管理、系统配置 |

---

## 三、数据库设计（MySQL）

### 3.1 核心表结构

#### 表 1：`users` —— 用户表
```sql
CREATE TABLE `users` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT COMMENT '主键ID',
  `openid` VARCHAR(64) NOT NULL COMMENT '微信openid',
  `unionid` VARCHAR(64) DEFAULT NULL COMMENT '微信unionid',
  `nickname` VARCHAR(64) DEFAULT NULL COMMENT '微信昵称',
  `avatar` VARCHAR(255) DEFAULT NULL COMMENT '头像URL',
  `phone` VARCHAR(20) DEFAULT NULL COMMENT '手机号',
  `birthday` DATE DEFAULT NULL COMMENT '生日',
  `is_shareholder` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否共享股东 0-否 1-是',
  `shareholder_level` TINYINT NOT NULL DEFAULT 0 COMMENT '股东等级 0-普通用户 1-初级股东',
  `shareholder_expire_at` DATETIME DEFAULT NULL COMMENT '股东身份过期时间（如有需要）',
  `register_fee_paid` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否已支付注册费',
  `free_drink_used` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '免费鸡尾酒是否已使用',
  `invite_code` VARCHAR(16) DEFAULT NULL COMMENT '个人邀请码',
  `parent_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '上级分享人ID',
  `parent_path` VARCHAR(500) DEFAULT NULL COMMENT '邀请路径，如 /1/5/20/',
  `balance` DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '佣金余额',
  `total_earning` DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '累计收益',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '状态 1-正常 0-禁用',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_openid` (`openid`),
  UNIQUE KEY `uk_invite_code` (`invite_code`),
  KEY `idx_parent_id` (`parent_id`),
  KEY `idx_is_shareholder` (`is_shareholder`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

#### 表 2：`shareholder_orders` —— 股东注册订单
```sql
CREATE TABLE `shareholder_orders` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '用户ID',
  `order_no` VARCHAR(32) NOT NULL COMMENT '订单编号 SH+年月日+6位随机',
  `amount` DECIMAL(10,2) NOT NULL COMMENT '支付金额（注册费）',
  `pay_status` TINYINT NOT NULL DEFAULT 0 COMMENT '支付状态 0-未支付 1-已支付 2-已退款',
  `pay_time` DATETIME DEFAULT NULL,
  `transaction_id` VARCHAR(64) DEFAULT NULL COMMENT '微信支付流水号',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='股东注册订单';
```

#### 表 3：`commission_records` —— 佣金记录
```sql
CREATE TABLE `commission_records` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `shareholder_id` BIGINT UNSIGNED NOT NULL COMMENT '获得佣金的股东ID',
  `consumer_id` BIGINT UNSIGNED NOT NULL COMMENT '消费者ID',
  `order_id` BIGINT UNSIGNED NOT NULL COMMENT '关联订单ID',
  `order_type` TINYINT NOT NULL COMMENT '订单类型 1-酒水订单 2-其他',
  `order_amount` DECIMAL(10,2) NOT NULL COMMENT '订单金额',
  `commission_rate` DECIMAL(4,2) NOT NULL COMMENT '佣金比例，如 0.10',
  `commission_amount` DECIMAL(10,2) NOT NULL COMMENT '佣金金额',
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '状态 0-待结算 1-已结算 2-已提现',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `settled_at` DATETIME DEFAULT NULL,
  PRIMARY KEY (`id`),
  KEY `idx_shareholder_id` (`shareholder_id`),
  KEY `idx_consumer_id` (`consumer_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='佣金记录';
```

#### 表 4：`tarot_cards` —— 塔罗牌库
```sql
CREATE TABLE `tarot_cards` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `card_no` INT NOT NULL COMMENT '牌编号 0-77',
  `name` VARCHAR(32) NOT NULL COMMENT '牌名，如"愚者"',
  `name_en` VARCHAR(64) DEFAULT NULL COMMENT '英文名',
  `arcana_type` TINYINT NOT NULL COMMENT '类型 1-大阿卡纳 2-小阿卡纳',
  `suit` VARCHAR(16) DEFAULT NULL COMMENT '花色 权杖/圣杯/宝剑/星币（小阿卡纳）',
  `image_url` VARCHAR(255) NOT NULL COMMENT '牌面图片',
  `upright_meaning` TEXT COMMENT '正位含义',
  `reversed_meaning` TEXT COMMENT '逆位含义',
  `keywords` VARCHAR(255) DEFAULT NULL COMMENT '关键词，逗号分隔',
  `element` VARCHAR(16) DEFAULT NULL COMMENT '元素属性 火/水/风/土',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_card_no` (`card_no`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='塔罗牌数据';
```

#### 表 5：`tarot_readings` —— 占卜记录
```sql
CREATE TABLE `tarot_readings` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `question` VARCHAR(255) DEFAULT NULL COMMENT '问题/意图',
  `spread_type` TINYINT NOT NULL DEFAULT 1 COMMENT '牌阵类型 1-单张牌',
  `cards` JSON NOT NULL COMMENT '抽到的牌 [{"card_no":0,"is_reversed":false,"position":"今日运势"}]',
  `recommended_drink_id` BIGINT UNSIGNED NOT NULL COMMENT '推荐酒水ID',
  `recommended_reason` VARCHAR(255) DEFAULT NULL COMMENT '推荐理由',
  `reading_date` DATE NOT NULL COMMENT '占卜日期',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_reading_date` (`reading_date`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='占卜记录';
```

#### 表 6：`drinks` —— 酒水表
```sql
CREATE TABLE `drinks` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(64) NOT NULL COMMENT '酒水名称',
  `english_name` VARCHAR(128) DEFAULT NULL,
  `category_id` INT UNSIGNED NOT NULL COMMENT '分类ID',
  `price` DECIMAL(8,2) NOT NULL COMMENT '售价',
  `cost_price` DECIMAL(8,2) DEFAULT NULL COMMENT '成本价',
  `alcohol` DECIMAL(4,1) DEFAULT NULL COMMENT '酒精度 %',
  `volume` VARCHAR(16) DEFAULT NULL COMMENT '容量，如"350ml"',
  `description` TEXT COMMENT '描述',
  `ingredients` VARCHAR(255) DEFAULT NULL COMMENT '配料',
  `image_url` VARCHAR(255) DEFAULT NULL,
  `is_recommended` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否推荐',
  `is_free_drink` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否可作为股东免费酒',
  `status` TINYINT NOT NULL DEFAULT 1 COMMENT '1-上架 0-下架',
  `sort_order` INT NOT NULL DEFAULT 0,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `updated_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_category_id` (`category_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='酒水表';
```

#### 表 7：`drink_categories` —— 酒水分类
```sql
CREATE TABLE `drink_categories` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `name` VARCHAR(32) NOT NULL COMMENT '分类名，如"鸡尾酒/威士忌"',
  `icon` VARCHAR(255) DEFAULT NULL,
  `sort_order` INT NOT NULL DEFAULT 0,
  `status` TINYINT NOT NULL DEFAULT 1,
  PRIMARY KEY (`id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='酒水分类';
```

#### 表 8：`tarot_drink_mappings` —— 塔罗-酒水映射
```sql
CREATE TABLE `tarot_drink_mappings` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `card_no` INT NOT NULL COMMENT '塔罗牌编号',
  `drink_id` BIGINT UNSIGNED NOT NULL COMMENT '酒水ID',
  `is_reversed` TINYINT(1) NOT NULL DEFAULT 0 COMMENT '是否逆位匹配',
  `match_score` INT NOT NULL DEFAULT 100 COMMENT '匹配分数',
  `reason_template` VARCHAR(255) DEFAULT NULL COMMENT '推荐理由模板',
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_card_drink_reversed` (`card_no`,`drink_id`,`is_reversed`),
  KEY `idx_card_no` (`card_no`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='塔罗牌与酒水推荐映射';
```

#### 表 9：`birthday_gifts` —— 生日礼品记录
```sql
CREATE TABLE `birthday_gifts` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `year` INT NOT NULL COMMENT '年份',
  `gift_type` TINYINT NOT NULL DEFAULT 1 COMMENT '礼品类型 1-实物 2-优惠券',
  `gift_name` VARCHAR(64) NOT NULL,
  `gift_value` DECIMAL(8,2) DEFAULT NULL COMMENT '礼品价值',
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未领取 1-已领取 2-已核销',
  `claimed_at` DATETIME DEFAULT NULL,
  `used_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_user_year` (`user_id`,`year`),
  KEY `idx_user_id` (`user_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='生日礼品记录';
```

#### 表 10：`reviews` —— 离店点评
```sql
CREATE TABLE `reviews` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `dianping_url` VARCHAR(255) DEFAULT NULL COMMENT '大众点评店铺链接',
  `screenshot_url` VARCHAR(255) NOT NULL COMMENT '点评截图',
  `review_content` TEXT COMMENT '评论内容（可选填）',
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '审核状态 0-待审核 1-已通过 2-已拒绝',
  `reject_reason` VARCHAR(255) DEFAULT NULL,
  `coupon_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '发放的优惠券ID',
  `audit_time` DATETIME DEFAULT NULL,
  `audit_admin_id` INT UNSIGNED DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='离店点评';
```

#### 表 11：`coupons` —— 优惠券
```sql
CREATE TABLE `coupons` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `user_id` BIGINT UNSIGNED NOT NULL,
  `coupon_no` VARCHAR(32) NOT NULL COMMENT '券编号',
  `type` TINYINT NOT NULL COMMENT '类型 1-点评返券 2-生日券 3-活动券',
  `name` VARCHAR(64) NOT NULL,
  `amount` DECIMAL(8,2) NOT NULL COMMENT '减免金额',
  `min_order_amount` DECIMAL(8,2) DEFAULT 0.00 COMMENT '最低消费',
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-未使用 1-已使用 2-已过期',
  `valid_start` DATE NOT NULL,
  `valid_end` DATE NOT NULL,
  `used_at` DATETIME DEFAULT NULL,
  `used_order_id` BIGINT UNSIGNED DEFAULT NULL,
  `source_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '来源记录ID（如点评ID）',
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_coupon_no` (`coupon_no`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_status` (`status`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='优惠券';
```

#### 表 12：`banners` —— 轮播图/展示图
```sql
CREATE TABLE `banners` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `title` VARCHAR(64) DEFAULT NULL,
  `image_url` VARCHAR(255) NOT NULL,
  `link_type` TINYINT NOT NULL DEFAULT 0 COMMENT '0-无链接 1-内部页面 2-外部链接',
  `link_value` VARCHAR(255) DEFAULT NULL,
  `position` TINYINT NOT NULL COMMENT '位置 1-首页轮播 2-展示页画廊',
  `sort_order` INT NOT NULL DEFAULT 0,
  `status` TINYINT NOT NULL DEFAULT 1,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  KEY `idx_position` (`position`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='轮播图与展示图';
```

#### 表 13：`admin_users` —— 商家管理员
```sql
CREATE TABLE `admin_users` (
  `id` INT UNSIGNED NOT NULL AUTO_INCREMENT,
  `username` VARCHAR(32) NOT NULL,
  `password` VARCHAR(255) NOT NULL COMMENT 'bcrypt加密',
  `nickname` VARCHAR(32) DEFAULT NULL,
  `role` TINYINT NOT NULL DEFAULT 1 COMMENT '1-店长 2-店员 3-超级管理员',
  `status` TINYINT NOT NULL DEFAULT 1,
  `last_login_at` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_username` (`username`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商家管理员';
```

#### 表 14：`drink_orders` —— 酒水订单（用于佣金计算）
```sql
CREATE TABLE `drink_orders` (
  `id` BIGINT UNSIGNED NOT NULL AUTO_INCREMENT,
  `order_no` VARCHAR(32) NOT NULL,
  `user_id` BIGINT UNSIGNED NOT NULL COMMENT '消费者',
  `shareholder_id` BIGINT UNSIGNED DEFAULT NULL COMMENT '关联的分享股东',
  `total_amount` DECIMAL(10,2) NOT NULL,
  `discount_amount` DECIMAL(10,2) NOT NULL DEFAULT 0.00,
  `pay_amount` DECIMAL(10,2) NOT NULL,
  `coupon_id` BIGINT UNSIGNED DEFAULT NULL,
  `status` TINYINT NOT NULL DEFAULT 0 COMMENT '0-待支付 1-已支付 2-制作中 3-已完成 4-已取消',
  `pay_time` DATETIME DEFAULT NULL,
  `created_at` DATETIME NOT NULL DEFAULT CURRENT_TIMESTAMP,
  PRIMARY KEY (`id`),
  UNIQUE KEY `uk_order_no` (`order_no`),
  KEY `idx_user_id` (`user_id`),
  KEY `idx_shareholder_id` (`shareholder_id`)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='酒水订单';
```

---

## 四、后端 API 接口设计（Go + Gin）

### 4.1 路由分组结构

```go
// main.go 路由注册逻辑
func setupRouter(r *gin.Engine) {
    api := r.Group("/api/v1")
    
    // 公共接口（无需登录）
    pub := api.Group("/public")
    {
        pub.GET("/banners", bannerHandler.List)          // 轮播图
        pub.GET("/drinks", drinkHandler.List)            // 酒水列表
        pub.GET("/drinks/:id", drinkHandler.Detail)      // 酒水详情
        pub.GET("/tarot/cards", tarotHandler.CardList)   // 塔罗牌库
    }
    
    // 微信小程序（需微信登录）
    wx := api.Group("/wx")
    wx.Use(middleware.WXAuth())
    {
        wx.POST("/login", wxAuthHandler.Login)           // 微信登录
        wx.GET("/user", userHandler.GetProfile)          // 用户信息
        wx.PUT("/user/birthday", userHandler.SetBirthday)// 设置生日
        
        // 共享股东
        wx.POST("/shareholder/apply", shareholderHandler.Apply)     // 申请成为股东
        wx.POST("/shareholder/pay", shareholderHandler.Pay)           // 支付注册费
        wx.GET("/shareholder/profile", shareholderHandler.Profile)  // 股东信息
        wx.GET("/shareholder/earnings", shareholderHandler.Earnings)// 收益明细
        wx.GET("/shareholder/team", shareholderHandler.Team)        // 我的团队
        
        // 塔罗
        wx.POST("/tarot/divine", tarotHandler.Divine)     // 占卜
        wx.GET("/tarot/history", tarotHandler.History)    // 历史记录
        
        // 生日有礼
        wx.GET("/birthday/gift", birthdayHandler.CheckGift)
        wx.POST("/birthday/claim", birthdayHandler.Claim)
        
        // 离店点评
        wx.POST("/review/submit", reviewHandler.Submit)
        wx.GET("/review/status", reviewHandler.Status)
        
        // 优惠券
        wx.GET("/coupons", couponHandler.MyCoupons)
        wx.POST("/coupons/use", couponHandler.UseCoupon)
        
        // 订单
        wx.POST("/orders", orderHandler.Create)
        wx.GET("/orders", orderHandler.List)
    }
    
    // 商家管理端（需 JWT）
    admin := api.Group("/admin")
    admin.Use(middleware.AdminAuth())
    {
        admin.POST("/login", adminHandler.Login)
        admin.GET("/dashboard", adminHandler.Dashboard)
        
        // 用户管理
        admin.GET("/users", adminUserHandler.List)
        admin.PUT("/users/:id/status", adminUserHandler.UpdateStatus)
        
        // 股东管理
        admin.GET("/shareholders", adminShareHandler.List)
        admin.GET("/shareholders/:id/earnings", adminShareHandler.Earnings)
        
        // 酒水管理
        admin.GET("/drinks", adminDrinkHandler.List)
        admin.POST("/drinks", adminDrinkHandler.Create)
        admin.PUT("/drinks/:id", adminDrinkHandler.Update)
        admin.DELETE("/drinks/:id", adminDrinkHandler.Delete)
        admin.GET("/categories", adminDrinkHandler.Categories)
        admin.POST("/categories", adminDrinkHandler.CreateCategory)
        
        // 塔罗管理
        admin.GET("/tarot/mappings", adminTarotHandler.Mappings)
        admin.PUT("/tarot/mappings", adminTarotHandler.UpdateMapping)
        
        // 轮播图/展示
        admin.GET("/banners", adminBannerHandler.List)
        admin.POST("/banners", adminBannerHandler.Create)
        admin.PUT("/banners/:id", adminBannerHandler.Update)
        admin.DELETE("/banners/:id", adminBannerHandler.Delete)
        
        // 点评审核
        admin.GET("/reviews", adminReviewHandler.List)
        admin.POST("/reviews/:id/audit", adminReviewHandler.Audit)
        
        // 订单管理
        admin.GET("/orders", adminOrderHandler.List)
        admin.PUT("/orders/:id/status", adminOrderHandler.UpdateStatus)
        
        // 系统设置
        admin.GET("/settings", adminSettingHandler.Get)
        admin.PUT("/settings", adminSettingHandler.Update)
    }
}
```

### 4.2 核心接口详细定义

#### 接口 1：微信登录
```http
POST /api/v1/wx/login
Content-Type: application/json

Request:
{
  "code": "wx_auth_code_from_js",
  "invite_code": "A1B2C3"  // 可选，通过邀请进入
}

Response:
{
  "code": 0,
  "data": {
    "token": "jwt_token_for_wx_user",
    "expire": 7200,
    "user_info": {
      "id": 10001,
      "nickname": "酒友",
      "avatar": "https://...",
      "is_shareholder": false,
      "is_new_user": true
    }
  }
}
```

#### 接口 2：发起股东注册支付
```http
POST /api/v1/wx/shareholder/pay
Authorization: Bearer <token>

Request:
{
  "amount": 99.00  // 注册费金额，后台可配置
}

Response:
{
  "code": 0,
  "data": {
    "order_no": "SH202504210001",
    "pay_params": {
      "appId": "wx...",
      "timeStamp": "1713680000",
      "nonceStr": "random_string",
      "package": "prepay_id=wx201410272009395522657a690399285100",
      "signType": "RSA",
      "paySign": "signature"
    }
  }
}
```

#### 接口 3：执行塔罗占卜
```http
POST /api/v1/wx/tarot/divine
Authorization: Bearer <token>

Request:
{
  "question": "今天适合喝什么？",  // 可选
  "spread_type": 1  // 1-单张牌
}

Response:
{
  "code": 0,
  "data": {
    "reading_id": 1001,
    "cards": [
      {
        "card_no": 0,
        "name": "愚者",
        "is_reversed": false,
        "image_url": "https://oss.../fool.jpg",
        "meaning": "新的开始，冒险精神，适合尝试创新鸡尾酒"
      }
    ],
    "recommend": {
      "drink_id": 5,
      "name": "迷雾森林",
      "english_name": "Misty Forest",
      "image_url": "https://oss.../drink.jpg",
      "price": 88.00,
      "reason": "愚者牌代表冒险与新生，这杯以烟熏威士忌为基底的创新鸡尾酒，正适合开启今晚的未知旅程。",
      "alcohol": 25.5
    }
  }
}
```

#### 接口 4：提交离店点评
```http
POST /api/v1/wx/review/submit
Authorization: Bearer <token>
Content-Type: multipart/form-data

Form Data:
- screenshot: [File]  // 点评截图
- content: "环境很棒，酒很好喝"  // 可选

Response:
{
  "code": 0,
  "data": {
    "review_id": 2001,
    "status": 0,  // 待审核
    "message": "审核通过后将自动发放优惠券"
  }
}
```

---

## 五、小程序端页面设计

### 5.1 页面结构（app.json）

```json
{
  "pages": [
    "pages/index/index",           // 首页
    "pages/showcase/showcase",     // 展示
    "pages/menu/menu",             // 酒水单
    "pages/profile/profile",       // 我的
    "pages/shareholder/apply",     // 股东申请
    "pages/shareholder/earnings",  // 收益明细
    "pages/shareholder/team",      // 我的团队
    "pages/tarot/divine",          // 塔罗占卜
    "pages/tarot/result",          // 占卜结果
    "pages/tarot/history",         // 历史记录
    "pages/birthday/gift",         // 生日礼品
    "pages/review/submit",         // 提交点评
    "pages/review/guide",          // 点评引导（跳转说明）
    "pages/drink/detail",          // 酒水详情
    "pages/coupon/list",           // 我的优惠券
    "pages/webview/webview"        // 外部网页（大众点评）
  ],
  "tabBar": {
    "list": [
      { "pagePath": "pages/index/index", "text": "首页", "iconPath": "...", "selectedIconPath": "..." },
      { "pagePath": "pages/showcase/showcase", "text": "展示", "iconPath": "...", "selectedIconPath": "..." },
      { "pagePath": "pages/menu/menu", "text": "酒水单", "iconPath": "...", "selectedIconPath": "..." },
      { "pagePath": "pages/profile/profile", "text": "我的", "iconPath": "...", "selectedIconPath": "..." }
    ],
    "selectedColor": "#D4AF37",
    "backgroundColor": "#1a1a1a",
    "borderStyle": "black"
  },
  "window": {
    "navigationBarBackgroundColor": "#1a1a1a",
    "navigationBarTitleText": "NoSocial",
    "navigationBarTextStyle": "white"
  }
}
```

### 5.2 首页（pages/index/index）组件结构

```
┌─────────────────────────────┐
│  <swiper> 轮播图              │  ← 自动播放5秒，点击跳转
│  [indicator-dots]            │
├─────────────────────────────┤
│  <grid> 功能入口 2x2         │
│  ┌─────────┐ ┌─────────┐   │
│  │  共享   │ │  生日   │   │
│  │  股东   │ │  有礼   │   │
│  └─────────┘ └─────────┘   │
│  ┌─────────┐ ┌─────────┐   │
│  │  酒水   │ │  离店   │   │
│  │  塔罗   │ │  点评   │   │
│  └─────────┘ └─────────┘   │
├─────────────────────────────┤
│  <section> 今日推荐         │
│  <drink-card>               │
├─────────────────────────────┤
│  <section> 店铺信息          │
│  地址/电话/营业时间          │
└─────────────────────────────┘
```

**首页功能入口点击逻辑：**
- **共享股东**：判断 `is_shareholder`，未注册跳转 `apply` 页，已注册跳转 `earnings`
- **生日有礼**：判断是否为股东 + 是否生日当天，跳转 `gift` 页
- **酒水塔罗**：跳转 `divine` 页
- **离店点评**：跳转 `guide` 页（先教用户怎么去大众点评点评，再回来上传截图）

### 5.3 塔罗占卜页交互流程

```
1. 进入 divine 页
2. 显示洗牌动画（CSS 3D变换）
3. 用户点击"抽牌"按钮
4. 前端调用 /api/v1/wx/tarot/divine
5. 后端算法：
   a. 随机抽取1张牌（0-77）
   b. 随机决定是否逆位（20%概率逆位）
   c. 根据 card_no + is_reversed 查询 tarot_drink_mappings
   d. 取 match_score 最高的酒水作为推荐
   e. 组合 reason_template + card_meaning 生成推荐理由
6. 返回结果，前端渲染 result 页
7. 显示：牌面（正/逆）、含义、推荐酒水卡片、一键下单按钮
```

### 5.4 共享股东功能详细逻辑

**注册流程：**
```
用户点击"共享股东" 
  → 检查登录状态
  → 未注册：显示介绍页（权益说明：1.分享得佣金 2.免费鸡尾酒 3.生日礼品）
     → 点击"立即加入" 
     → 调用 /shareholder/pay 获取微信支付参数
     → 调起微信支付
     → 支付成功回调：更新 user.is_shareholder=1, register_fee_paid=1
     → 生成 invite_code（6位字母数字，如 A1B2C3）
     → 显示成功页，展示邀请码和分享按钮
  → 已注册：显示股东中心
     → 累计收益 / 可提现余额 / 我的团队人数
     → 分享按钮（生成小程序码携带 scene=invite_code）
```

**佣金计算规则（后台可配置）：**
- 一级佣金比例：10%（消费者通过你的邀请码注册并消费，你获得10%）
- 结算时机：订单完成（确认收货/离店）后 T+1 结算
- 提现门槛：满 50 元可提现，手续费 1%

**分享链路：**
```
分享人生成带参数的小程序码（scene=INVITE_CODE）
  → 被分享人扫码进入小程序
  → 在 wx.login 时携带 invite_code
  → 后端将 parent_id 设为分享人ID
  → 被分享人消费时，查找其 parent_id 的 shareholder_id
  → 按订单金额 * commission_rate 计算佣金
```

### 5.5 离店点评流程

```
用户点击"离店点评"
  → 进入 guide 页
  → 步骤说明：
    1. 点击按钮跳转大众点评APP/网页（wx.navigateToMiniProgram 或 web-view）
    2. 在大众点评搜索"NoSocial"并写5星好评
    3. 截图保存评价页面
    4. 返回小程序，上传截图
  → 用户点击"去点评"：保存当前订单/访问记录，跳转外部
  → 用户返回后进入 submit 页
  → 上传截图 + 可选填评价内容
  → 提交后状态为"待审核"
  → 管理端审核通过：自动生成优惠券（如 20元无门槛券，有效期30天）
  → 小程序推送通知：审核通过，优惠券已到账
```

---

## 六、商家管理端设计（Vue 3）

### 6.1 项目结构

```
admin/
├── src/
│   ├── api/                 # 接口封装（按模块）
│   │   ├── user.js
│   │   ├── shareholder.js
│   │   ├── drink.js
│   │   ├── order.js
│   │   ├── review.js
│   │   ├── tarot.js
│   │   └── setting.js
│   ├── components/          # 公共组件
│   │   ├── AppHeader.vue
│   │   ├── Sidebar.vue
│   │   ├── ImageUpload.vue  # 封装OSS上传
│   │   └── StatCard.vue
│   ├── views/               # 页面
│   │   ├── login/
│   │   ├── dashboard/       # 数据看板
│   │   ├── user/            # 用户列表
│   │   ├── shareholder/     # 股东管理
│   │   ├── drink/           # 酒水管理
│   │   │   ├── List.vue
│   │   │   ├── Category.vue
│   │   │   └── Form.vue     # 新增/编辑
│   │   ├── order/           # 订单管理
│   │   ├── review/          # 点评审核
│   │   ├── tarot/           # 塔罗配置
│   │   ├── banner/          # 轮播图管理
│   │   └── setting/         # 系统设置
│   ├── router/
│   │   └── index.js         # 路由配置 + 权限守卫
│   ├── stores/
│   │   └── user.js          # Pinia 用户状态
│   ├── utils/
│   │   ├── request.js       # Axios 封装（JWT自动附加）
│   │   └── constants.js     # 枚举值
│   └── App.vue
├── vite.config.js
└── package.json
```

### 6.2 路由配置

```javascript
const routes = [
  {
    path: '/login',
    component: () => import('@/views/login/index.vue'),
    meta: { public: true }
  },
  {
    path: '/',
    component: Layout,
    redirect: '/dashboard',
    children: [
      { path: 'dashboard', component: () => import('@/views/dashboard/index.vue'), meta: { title: '数据看板', icon: 'Dashboard' } },
      { path: 'users', component: () => import('@/views/user/List.vue'), meta: { title: '用户管理', icon: 'User' } },
      { path: 'shareholders', component: () => import('@/views/shareholder/List.vue'), meta: { title: '股东管理', icon: 'Money' } },
      { path: 'drinks', component: () => import('@/views/drink/List.vue'), meta: { title: '酒水管理', icon: 'Coffee' } },
      { path: 'orders', component: () => import('@/views/order/List.vue'), meta: { title: '订单管理', icon: 'List' } },
      { path: 'reviews', component: () => import('@/views/review/List.vue'), meta: { title: '点评审核', icon: 'ChatDotRound' } },
      { path: 'tarot', component: () => import('@/views/tarot/Config.vue'), meta: { title: '塔罗配置', icon: 'MagicStick' } },
      { path: 'banners', component: () => import('@/views/banner/List.vue'), meta: { title: '图片管理', icon: 'Picture' } },
      { path: 'settings', component: () => import('@/views/setting/index.vue'), meta: { title: '系统设置', icon: 'Setting' } }
    ]
  }
]
```

### 6.3 核心页面说明

#### 数据看板（Dashboard）
- **统计卡片**：今日营业额、今日订单数、新增股东数、待审核点评数
- **图表**：近7天营业额趋势（ECharts 折线图）、酒水销量排行（柱状图）
- **快捷入口**：审核待处理的点评、查看库存预警

#### 酒水管理
- **列表页**：支持按分类筛选、上下架状态筛选、价格排序
- **表单页**：名称/英文名/分类/价格/成本/酒精度/容量/配料/描述/图片上传/是否推荐/是否免费酒
- **分类管理**：拖拽排序、图标上传

#### 塔罗配置页
- **牌库管理**：78张牌的图片上传、正逆位含义编辑
- **映射配置**：为每张牌配置匹配的酒水（正位推荐、逆位推荐分别设置）
- **推荐理由模板**：支持变量替换，如 `{{card_name}}代表{{keyword}}，这杯{{drink_name}}正适合{{scene}}`

#### 点评审核页
- **列表**：待审核/已通过/已拒绝 标签切换
- **审核操作**：查看大图、通过（自动发券）/拒绝（填写原因）
- **批量审核**：支持批量通过

---

## 七、核心业务逻辑详解（Go 实现要点）

### 7.1 塔罗占卜算法（tarot/divine）

```go
// 算法逻辑说明（伪代码）
func Divine(userID int64, question string) (*TarotResult, error) {
    // 1. 生成随机种子（当前时间纳秒 + userID）
    seed := time.Now().UnixNano() + userID
    r := rand.New(rand.NewSource(seed))
    
    // 2. 抽取1张牌（0-77）
    cardNo := r.Intn(78)
    
    // 3. 20%概率逆位
    isReversed := r.Float32() < 0.2
    
    // 4. 查询牌信息
    card := tarotRepo.GetCard(cardNo)
    
    // 5. 查询映射关系，取匹配度最高的酒水
    mappings := tarotRepo.GetMappings(cardNo, isReversed)
    // 按 match_score 降序，取第一条
    mapping := mappings[0]
    drink := drinkRepo.GetByID(mapping.DrinkID)
    
    // 6. 生成推荐理由
    meaning := card.UprightMeaning
    if isReversed {
        meaning = card.ReversedMeaning
    }
    reason := fmt.Sprintf(
        "【%s】%s代表%s。%s",
        card.Name,
        map[bool]string{true: "逆位", false: "正位"}[isReversed],
        card.Keywords,
        mapping.ReasonTemplate,
    )
    
    // 7. 保存记录
    reading := &TarotReading{
        UserID: userID,
        Cards: []ReadingCard{{
            CardNo: cardNo,
            IsReversed: isReversed,
            Position: "今日运势",
        }},
        RecommendedDrinkID: drink.ID,
        RecommendedReason: reason,
        ReadingDate: time.Now().Format("2006-01-02"),
    }
    tarotRepo.SaveReading(reading)
    
    return &TarotResult{
        ReadingID: reading.ID,
        Card: card,
        IsReversed: isReversed,
        Meaning: meaning,
        Drink: drink,
        Reason: reason,
    }, nil
}
```

### 7.2 佣金计算与结算（异步任务）

```go
// 订单完成后触发
func CalculateCommission(order *DrinkOrder) error {
    if order.ShareholderID == 0 {
        return nil // 无关联股东
    }
    
    // 查询股东信息
    shareholder := userRepo.GetByID(order.ShareholderID)
    if !shareholder.IsShareholder {
        return nil
    }
    
    // 查询佣金比例配置（从 settings 表）
    rate := settingRepo.GetFloat("commission_rate") // 默认 0.10
    
    commission := order.PayAmount * rate
    
    // 创建佣金记录（待结算状态）
    record := &CommissionRecord{
        ShareholderID: order.ShareholderID,
        ConsumerID: order.UserID,
        OrderID: order.ID,
        OrderType: 1,
        OrderAmount: order.PayAmount,
        CommissionRate: rate,
        CommissionAmount: commission,
        Status: 0, // 待结算
    }
    
    // 事务：插入记录 + 更新用户累计收益（未到账余额）
    err := db.Transaction(func(tx *gorm.DB) error {
        if err := tx.Create(record).Error; err != nil {
            return err
        }
        if err := tx.Model(&User{}).Where("id = ?", order.ShareholderID).
            Update("total_earning", gorm.Expr("total_earning + ?", commission)).Error; err != nil {
            return err
        }
        return nil
    })
    
    return err
}

// 定时任务（每日凌晨1点）：将N+1天前的待结算佣金转为可提现余额
func SettleCommissions() {
    deadline := time.Now().AddDate(0, 0, -1) // T+1
    records := commissionRepo.GetPendingByDate(deadline)
    
    for _, r := range records {
        db.Transaction(func(tx *gorm.DB) error {
            tx.Model(&CommissionRecord{}).Where("id = ?", r.ID).Update("status", 1)
            tx.Model(&User{}).Where("id = ?", r.ShareholderID).
                Update("balance", gorm.Expr("balance + ?", r.CommissionAmount))
            return nil
        })
    }
}
```

### 7.3 生日礼品自动发放（定时任务）

```go
// 每日 00:05 执行
func CheckBirthdayGifts() {
    today := time.Now().Format("01-02")
    year := time.Now().Year()
    
    // 查询今天生日的股东
    users := userRepo.GetShareholdersByBirthday(today)
    
    for _, u := range users {
        // 幂等检查：今年是否已发放
        exists := birthdayRepo.Exists(u.ID, year)
        if exists {
            continue
        }
        
        gift := &BirthdayGift{
            UserID: u.ID,
            Year: year,
            GiftType: 1, // 实物礼品
            GiftName: "生日特调鸡尾酒一杯",
            GiftValue: 88.00,
            Status: 0, // 未领取
        }
        birthdayRepo.Create(gift)
        
        // 发送订阅消息通知用户
        wxMsgService.SendBirthdayGiftNotice(u.OpenID, gift.GiftName)
    }
}
```

### 7.4 离店点评审核通过后的优惠券发放

```go
func AuditReview(reviewID int64, adminID uint, pass bool, reason string) error {
    review := reviewRepo.GetByID(reviewID)
    
    if !pass {
        return reviewRepo.UpdateStatus(reviewID, 2, reason)
    }
    
    // 通过：创建优惠券
    coupon := &Coupon{
        UserID: review.UserID,
        CouponNo: generateCouponNo(),
        Type: 1, // 点评返券
        Name: "点评感谢券",
        Amount: 20.00, // 后台配置
        MinOrderAmount: 0,
        Status: 0,
        ValidStart: time.Now(),
        ValidEnd: time.Now().AddDate(0, 0, 30),
        SourceID: review.ID,
    }
    
    return db.Transaction(func(tx *gorm.DB) error {
        if err := tx.Create(coupon).Error; err != nil {
            return err
        }
        if err := tx.Model(&Review{}).Where("id = ?", reviewID).
            Updates(map[string]interface{}{
                "status": 1,
                "coupon_id": coupon.ID,
                "audit_time": time.Now(),
                "audit_admin_id": adminID,
            }).Error; err != nil {
            return err
        }
        return nil
    })
}
```

---

## 八、微信小程序端技术细节

### 8.1 全局状态管理（使用 MobX / 原生方案）

由于小程序原生开发，建议使用 **全局数据管理方案**：

```javascript
// stores/app.js
import { observable, action } from 'mobx-miniprogram'

export const appStore = observable({
  // 数据字段
  token: '',
  userInfo: null,
  isShareholder: false,
  
  // 计算属性
  get isLogin() {
    return !!this.token
  },
  
  // actions
  setToken: action(function(token) {
    this.token = token
    wx.setStorageSync('token', token)
  }),
  
  setUserInfo: action(function(info) {
    this.userInfo = info
    this.isShareholder = info.is_shareholder
  })
})
```

### 8.2 网络请求封装

```javascript
// utils/request.js
const BASE_URL = 'https://api.nosocial.bar/api/v1'

const request = (options) => {
  return new Promise((resolve, reject) => {
    wx.request({
      url: `${BASE_URL}${options.url}`,
      method: options.method || 'GET',
      data: options.data,
      header: {
        'Authorization': `Bearer ${wx.getStorageSync('token')}`,
        'Content-Type': 'application/json'
      },
      success: (res) => {
        if (res.statusCode === 401) {
          wx.navigateTo({ url: '/pages/login/login' })
          return reject(new Error('登录过期'))
        }
        if (res.data.code !== 0) {
          wx.showToast({ title: res.data.msg || '请求失败', icon: 'none' })
          return reject(res.data)
        }
        resolve(res.data.data)
      },
      fail: reject
    })
  })
}

export const get = (url, params) => request({ url, method: 'GET', data: params })
export const post = (url, data) => request({ url, method: 'POST', data })
```

### 8.3 图片上传（直传 OSS）

```javascript
// 后端提供 STS 临时凭证
async function uploadImage(filePath) {
  // 1. 获取 OSS 临时凭证
  const cred = await get('/wx/oss/sts')
  
  // 2. 使用 wx.uploadFile 直传 OSS
  return new Promise((resolve, reject) => {
    wx.uploadFile({
      url: cred.host,
      filePath: filePath,
      name: 'file',
      formData: {
        key: cred.key,
        policy: cred.policy,
        OSSAccessKeyId: cred.accessKeyId,
        signature: cred.signature,
        'x-oss-security-token': cred.securityToken
      },
      success: (res) => {
        if (res.statusCode === 200 || res.statusCode === 204) {
          resolve(`${cred.host}/${cred.key}`)
        } else {
          reject(new Error('上传失败'))
        }
      }
    })
  })
}
```

---

## 九、部署与运维方案

### 9.1 Docker Compose 部署文件

```yaml
# docker-compose.yml
version: '3.8'

services:
  app:
    build: ./backend
    ports:
      - "8080:8080"
    environment:
      - DB_DSN=root:password@tcp(mysql:3306)/nosocial?charset=utf8mb4
      - REDIS_ADDR=redis:6379
      - WX_APPID=${WX_APPID}
      - WX_SECRET=${WX_SECRET}
      - OSS_ACCESS_KEY=${OSS_ACCESS_KEY}
      - OSS_SECRET=${OSS_SECRET}
    depends_on:
      - mysql
      - redis
    restart: always

  admin:
    build: ./admin
    ports:
      - "80:80"
    depends_on:
      - app
    restart: always

  mysql:
    image: mysql:8.0
    environment:
      MYSQL_ROOT_PASSWORD: password
      MYSQL_DATABASE: nosocial
    volumes:
      - mysql_data:/var/lib/mysql
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "3306:3306"

  redis:
    image: redis:6-alpine
    volumes:
      - redis_data:/data

volumes:
  mysql_data:
  redis_data:
```

### 9.2 环境变量配置（.env）

```bash
# 微信
WX_APPID=wx1234567890
WX_SECRET=your_secret_here
WX_MCH_ID=商户号
WX_APIV3_KEY=APIv3密钥

# 数据库
DB_HOST=mysql
DB_PORT=3306
DB_USER=root
DB_PASSWORD=password
DB_NAME=nosocial

# Redis
REDIS_HOST=redis
REDIS_PORT=6379

# 阿里云 OSS
OSS_ENDPOINT=oss-cn-beijing.aliyuncs.com
OSS_BUCKET=nosocial-images
OSS_ACCESS_KEY=LTAI...
OSS_SECRET=your_secret

# 业务配置
SHAREHOLDER_FEE=99.00
COMMISSION_RATE=0.10
FREE_DRINK_ID=1  # 注册免费赠送的酒水ID
```

---

## 十、开发排期与里程碑

| 阶段 | 周期 | 交付物 |
|------|------|--------|
| **Phase 1** | Week 1-2 | 数据库设计、后端基础框架、微信登录、OSS上传 |
| **Phase 2** | Week 3-4 | 用户模块、酒水模块、轮播图、首页开发 |
| **Phase 3** | Week 5-6 | 共享股东系统（支付、邀请、佣金计算） |
| **Phase 4** | Week 7-8 | 塔罗占卜系统（78张牌数据、占卜算法、结果页） |
| **Phase 5** | Week 9-10 | 生日有礼、离店点评、优惠券系统 |
| **Phase 6** | Week 11-12 | 商家管理端（Vue）、数据统计、审核后台 |
| **Phase 7** | Week 13 | 联调测试、性能优化、部署上线 |

---

## 十一、给 AI 写代码的 Prompt 模板

你可以将以下段落直接复制给 AI 作为编程指令：

### 11.1 生成 Go 后端代码的 Prompt

```
请基于以下要求生成 Go + Gin + GORM 的完整模块代码：

【项目背景】
酒吧小程序后端，使用 Go 1.22 + Gin + GORM + MySQL 8.0。

【当前任务】
实现 [共享股东模块] 的完整代码，包含：
1. Model 层：定义 ShareholderOrder、CommissionRecord 结构体，包含 JSON tags 和 GORM tags
2. Repository 层：实现 CRUD 和分页查询
3. Service 层：实现支付下单、支付回调、佣金计算逻辑
4. Handler 层：实现 HTTP API（Apply、Pay、Callback、Profile、Earnings、Team）
5. 路由注册：添加到 /api/v1/wx/shareholder/* 
6. 使用事务处理支付和佣金发放

【数据库表结构】
（粘贴上面的 SQL）

【接口定义】
（粘贴上面的接口定义）

【要求】
- 代码结构清晰，分层明确（model/dao/service/handler）
- 包含输入参数校验（使用 validator）
- 错误处理统一返回 JSON {code, msg, data}
- 包含详细的注释说明
- 使用依赖注入或构造函数初始化各层
```

### 11.2 生成 Vue 管理端的 Prompt

```
请基于以下要求生成 Vue 3 + Element Plus 的完整页面代码：

【项目背景】
酒吧小程序商家管理后台，使用 Vue 3 + Vite + Element Plus + Pinia + Axios。

【当前任务】
实现 [酒水管理] 模块，包含：
1. 酒水列表页：表格展示、分类筛选、状态筛选、分页、上下架开关
2. 酒水表单页：新增/编辑对话框，包含表单验证、图片上传（OSS直传）
3. 分类管理页：分类列表、拖拽排序、新增/编辑/删除

【接口定义】
GET /api/v1/admin/drinks?page=1&size=10&category_id=0&status=1
POST /api/v1/admin/drinks
PUT /api/v1/admin/drinks/:id
DELETE /api/v1/admin/drinks/:id
GET /api/v1/admin/categories
POST /api/v1/admin/categories

【要求】
- 使用 Composition API + <script setup> 语法
- 表单使用 Element Plus 的 el-form 组件，包含 rules 验证
- 图片上传组件封装为独立组件 ImageUpload，支持预览和删除
- 使用 Pinia 管理分类数据（避免重复请求）
- 列表页支持批量操作
- 代码注释使用中文
```

---

这份文档涵盖了从数据库设计到前后端实现、从业务逻辑到部署运维的完整细节。你可以根据开发进度，将各章节分别作为 Prompt 投喂给 AI 进行代码生成。如需我进一步细化某个模块（如塔罗占卜的 78 张牌数据初始化 SQL、微信支付完整回调逻辑、或 Vue 管理端的完整路由守卫代码），随时告诉我。